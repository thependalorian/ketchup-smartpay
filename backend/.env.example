# Ketchup SmartPay Backend – required for deploy and portal connectivity
# Copy to .env.local and set values. Deploy script syncs .env.local to Vercel production.

# Required: Neon PostgreSQL (backend connects; portals call API only)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Required for portals: API keys (must match VITE_API_KEY in each portal)
# Government Portal: set VITE_API_KEY to this value in government-portal
GOVERNMENT_API_KEY=your_government_api_key

# Ketchup Portal: set VITE_API_KEY to one of these in ketchup-portal
KETCHUP_API_KEY=your_ketchup_api_key
# Or: KETCHUP_SMARTPAY_API_KEY / API_KEY (auth middleware accepts any)

# Optional: Buffr integration
BUFFR_API_URL=https://pay.buffr.ai
BUFFR_API_KEY=
BUFFR_WEBHOOK_SECRET=

# Optional: Open Banking / compliance / comms
# BON_PARTICIPANT_ID, FIRST_NAMIBIA_PARTICIPANT_ID, NEDBANK_PARTICIPANT_ID, STANDARD_BANK_PARTICIPANT_ID
# PAYMENT_STATUS_WEBHOOK_URL, SMS_GATEWAY_URL, USSD_GATEWAY_URL, BUFFR_NOTIFY_URL
# CAPITAL_* (compliance), NAMQR_SIGNATURE_SECRET, IPS_GATEWAY_URL, IPS_API_KEY
