{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd buffr_ai_ts && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd buffr_ai_ts && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

